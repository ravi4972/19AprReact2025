import React from 'react';
import ReactDOM from 'react-dom';

const root = ReactDOM.createRoot(document.getElementById("root"))

const header = React.createElement("h1",{},"React with parcel")

root.render(header)
